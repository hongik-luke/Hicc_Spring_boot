spring:
  datasource:
    url: jdbc:mariadb://localhost:9090/jpa_study
    username: anyeasulhea
    password: 1234
    driver-class-name: org.mariadb.jdbc.Driver

  sql:
    init:
      mode: never

  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MariaDBDialect
        show_sql: true
        format_sql: true
        use_sql_comments: true
        hbm2ddl:
          auto: create
        default_batch_fetch_size: 1000
