# spring.application.name=erp

spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: ${MY_DB_URL} #???? ??
    username: ${MY_DB_ID:root}
    password: ${MY_DB_PASSWORD}

  jpa:
    hibernate:
      ddl-auto: update # ?? ???? Entity ??? ?? ? DB ???? ?? ????.
    show-sql: true     # ???? ?? SQL ??? ?? ?? ????.
    properties:
      hibernate:
        format_sql: true # SQL ??? ????? ?? ?? ????.
